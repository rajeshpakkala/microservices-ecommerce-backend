server:
  port: 8080

spring:
  application:
    name: API-GATEWAY

  cloud:
    gateway:
      routes:
        - id: test-service
          uri: https://httpbin.org
          predicates:
            - Path=/test/**

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka
server:
  port: 8085

spring:
  application:
    name: NOTIFICATION-SERVICE

  datasource:
    url: jdbc:postgresql://localhost:5432/ecommerce_notification_db
    username: postgres
    password: mypassword

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

  kafka:
    bootstrap-servers: localhost:9092

  mail:
    host: smtp.gmail.com
    port: 587
    username: rajeshmbu87@gmail.com
    password: jydwznsusqyhyejq
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

auth:
  service:
    url: http://localhost:8082
